{
  "name": "agent-ops-e2e",
  "version": "1.0.0",
  "description": "E2E tests for Agent Ops with Aspire integration",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
